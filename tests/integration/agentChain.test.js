// tests/integration/agentChain.test.js
// Integration tests for the entire agent chain

const { runAgentChain } = require('../../src/index')

describe('Agent Chain Integration', () => {
  test('should process input through entire agent chain', async () => {
    // TODO: Implement integration test for the entire agent chain
    // Example:
    // const input = 'Sample input text';
    // const result = await runAgentChain(input);
    // expect(result).toHaveProperty('sentiment');
    // expect(result).toHaveProperty('topics');
  })
})
